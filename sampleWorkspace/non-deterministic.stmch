StateMachine SampleMachine {
    INITIAL -> A;

    state A {
        -> B ['a'];
        -> FINAL ['final'];
    }

    state B {
        -> C ['b'];
        -> D ['b'];
    }

    state C {
        -> A ['c'];
    }

    state D {
        -> A ['d'];
    }
}