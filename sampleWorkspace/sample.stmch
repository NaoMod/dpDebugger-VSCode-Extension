StateMachine SampleMachine {
    INITIAL -> A;

    state A {
        -> B ['b'];
    }

    composite state B {
        -> A ['a'];
        -> FINAL ['final'];

        INITIAL -> C;

        state C {
            -> D ['d'];
        }

        state D {
            -> C ['c'];
        }
    }
}